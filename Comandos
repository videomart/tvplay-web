# Rebuild completo (tudo)
docker-compose up -d --build

# Só o backend
docker-compose up -d --build api

# Só o frontend
docker-compose up -d --build web

# Só reiniciar (sem rebuild — quando não mudou código)
docker-compose restart

# Ver logs em tempo real
docker logs tvplay_api -f
docker logs tvplay_web -f


# Para dar pull no gitHUB
git add -A && git commit -m "mensagem" && git push origin main

# Atualizar em produção (rodar no servidor via SSH)
cd /home/bolais/projects/tvplay-web
git pull origin main
VITE_APP_BUILD=$(git rev-parse --short HEAD) docker compose up -d --build

# Ou tudo de uma vez (uma linha)
cd /home/bolais/projects/tvplay-web && git pull origin main && VITE_APP_BUILD=$(git rev-parse --short HEAD) docker compose up -d --build
