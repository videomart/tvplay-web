# Rebuild completo (tudo)
docker-compose up -d --build

# Só o backend
docker-compose up -d --build api

# Só o frontend
docker-compose up -d --build web

# Só reiniciar (sem rebuild — quando não mudou código)
docker-compose restart

# Ver logs em tempo real
docker logs tvplay_api -f
docker logs tvplay_web -f


#Para atualizar no cumputador local e enviar para o GitHUB
git add -A && git commit -m "mensagem" && git push origin main

#2 No servidor de produção via SSH
# CACHE_BUST garante que o frontend é sempre recompilado com o BUILD_TIME atualizado

cd /home/bolais/projects/tvplay-web && git pull origin main && CACHE_BUST=$(date +%s) docker compose up -d --build
