# Rebuild completo (tudo)
docker-compose up -d --build

# Só o backend
docker-compose up -d --build api

# Só o frontend
docker-compose up -d --build web

# Só reiniciar (sem rebuild — quando não mudou código)
docker-compose restart

# Ver logs em tempo real
docker logs tvplay_api -f
docker logs tvplay_web -f


# Para dar pull no gitHUB
git add -A && git commit -m "mensagem" && git push origin main

# Atualizar em produção (rodar no servidor via SSH)
cd /home/bolais/projects/tvplay-web && git pull origin main && docker compose up -d --build

# Para lançar uma nova versão (antes do git push):
# 1. Edite frontend/package.json e bumpe "version": "1.0.0" → "1.1.0"
# 2. git add -A && git commit -m "release: v1.1.0" && git push
# 3. No servidor: cd /home/bolais/projects/tvplay-web && git pull origin main && docker compose up -d --build
