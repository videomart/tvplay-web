# Rebuild completo (tudo)
docker-compose up -d --build

# Só o backend
docker-compose up -d --build api

# Só o frontend
docker-compose up -d --build web

# Só reiniciar (sem rebuild — quando não mudou código)
docker-compose restart

# Ver logs em tempo real
docker logs tvplay_api -f
docker logs tvplay_web -f


# Para dar pull no gitHUB
git add -A && git commit -m "mensagem" && git push origin main

# para atualizar na produção
git -C /home/bolais/projects/tvplay-web pull origin main

# Depois rebuild
cd /home/bolais/projects/tvplay-web && docker compose up -d --build

# Ou Tudo de vez
git -C /home/bolais/projects/tvplay-web pull origin main && docker compose -f /home/bolais/projects/tvplay-web/docker-compose.yml --project-directory /home/bolais/projects/tvplay-web up -d --build
